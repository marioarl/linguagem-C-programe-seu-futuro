#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    int i;

    //Imprimindo numeros impares de 1 ate 100
    for (i = 1; i <=100; i+=2){ //O mesmo que i = i + 2
        printf("%d ", i);
    }
    
    //Imprimindo numeros multiplos de 5 até 100
    for (i = 1; i <=100; i+=2){ //O mesmo que i = i + 2
        printf("%d ", i);
    }

    return 0;
}
